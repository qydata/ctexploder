import moment from 'moment'
require('moment/locale/zh-cn')
import numeral from 'numeral'
import 'numeral/locales'

// export const locale = 'en'
export const locale = 'en'  // 新标准简体中文

// moment.locale(locale)
numeral.locale(locale)
